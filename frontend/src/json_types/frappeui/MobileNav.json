{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "MobileNavProps": {
      "type": "object",
      "additionalProperties": false,
      "description": "`MobileNav` (the bar) has no props — it's a grid frame for `MobileNavItem`s."
    }
  }
}