{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "MobileShellProps": {
      "type": "object",
      "additionalProperties": false,
      "description": "`MobileShell` has no props — it's a pure slot frame (default slot = scrolling content, `#nav` slot = bottom navigation). This interface exists so the docs PropsTable and `defineProps` share one source of truth as props are added."
    }
  }
}