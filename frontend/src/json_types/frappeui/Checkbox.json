{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "CheckboxProps": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "Label rendered above (or beside, for binary controls) the input."
        },
        "description": {
          "type": "string",
          "description": "Helper text rendered below the input. Hidden when `error` is set."
        },
        "error": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                },
                "message": {
                  "type": "string"
                },
                "stack": {
                  "type": "string"
                },
                "messages": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "additionalProperties": false,
              "required": [
                "message",
                "name"
              ],
              "description": "Library-level extension of the standard `Error` type. Frappe's whitelisted methods may return an error with multiple messages on `messages?: string[]`. This is the shape input components render in their error region."
            }
          ],
          "description": "Error message rendered below the input. When set, the control receives `aria-invalid=\"true\"` and `data-state=\"invalid\"`. May be either a string or an `Error` object whose `messages?: string[]` is rendered as stacked lines (with `Error.message` as the fallback)."
        },
        "required": {
          "type": "boolean",
          "description": "Marks the field as required. Renders an asterisk next to the label and forwards `required` / `aria-required` to the underlying control."
        },
        "id": {
          "type": "string",
          "description": "HTML id of the underlying control. Auto-generated via `useId()` if omitted."
        },
        "size": {
          "type": "string",
          "enum": [
            "sm",
            "md"
          ],
          "description": "Controls the size of the checkbox"
        },
        "disabled": {
          "type": "boolean",
          "description": "Disables the checkbox interaction"
        },
        "padding": {
          "type": "boolean",
          "description": "Adds padding around the checkbox.",
          "deprecated": "Use `data-*` styling hooks instead."
        },
        "modelValue": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "number",
              "const": 1
            },
            {
              "type": "number",
              "const": 0
            }
          ],
          "description": "Checked state of the checkbox. `boolean` is canonical; `1`/`0` are kept for v1 backwards compatibility."
        },
        "indeterminate": {
          "type": "boolean",
          "description": "Renders the mixed \"—\" state (e.g. a select-all that's partially selected). Purely visual — the native `indeterminate` DOM property is not reflected as an attribute, so it must be set via this prop, not markup."
        }
      },
      "additionalProperties": false
    },
    "CheckboxEmits": {
      "type": "object",
      "properties": {
        "update:modelValue": {
          "type": "array",
          "items": {
            "type": "boolean",
            "title": "value"
          },
          "minItems": 1,
          "maxItems": 1,
          "description": "Fired when the checkbox value changes."
        }
      },
      "required": [
        "update:modelValue"
      ],
      "additionalProperties": false
    }
  }
}