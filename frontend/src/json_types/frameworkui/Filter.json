{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "FilterProps": {
      "type": "object",
      "properties": {
        "doctype": {
          "type": "string",
          "description": "Doctype whose Meta drives the filterable fields and per-fieldtype operators."
        }
      },
      "required": [
        "doctype"
      ],
      "additionalProperties": false,
      "description": "Props for `<Filter>`. The conditions are a separate `v-model` (`Filter[]`)."
    }
  }
}