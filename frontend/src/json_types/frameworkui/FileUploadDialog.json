{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "FileUploadDialogProps": {
      "type": "object",
      "properties": {
        "open": {
          "type": "boolean"
        },
        "multiple": {
          "type": "boolean"
        },
        "imageOnly": {
          "type": "boolean"
        },
        "crop": {
          "type": "boolean"
        },
        "restrictions": {
          "type": "object",
          "properties": {
            "max_file_size": {
              "type": [
                "number",
                "null"
              ]
            },
            "max_number_of_files": {
              "type": [
                "number",
                "null"
              ]
            },
            "allowed_file_types": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "crop_image_aspect_ratio": {
              "type": [
                "number",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "description": "Upload restrictions, in desk's shape (`max_file_size` in bytes, `allowed_file_types` as extensions/MIME globs). Enforced client-side by the uploader before any bytes leave the browser."
        },
        "transport": {
          "type": "object",
          "description": "The single seam between the queue and a backend. Receives one file plus its args and a context carrying an `AbortSignal` (cancel) and an `onProgress` callback (bytes loaded / total). Resolves with at least the resulting `file_url`. Swap this out for non-Frappe backends; the default wraps frappe-ui's `useFileUpload` and ports desk's chunked loop for large files."
        },
        "progressMode": {
          "type": "string",
          "enum": [
            "inline",
            "tray",
            "field",
            "toast"
          ],
          "description": "Which view renders an upload's progress:  - `inline`  — per-row bar + footer summary (dialog default)  - `tray`    — minimized floating card that survives the dialog closing  - `field`   — surface spinner on the consuming field  - `toast`   — terminal state when the user left both dialog and tray"
        },
        "folder": {
          "type": "string"
        },
        "title": {
          "type": "string",
          "description": "Accepted for API compatibility with the tabbed dialog; not rendered here."
        },
        "trayLabel": {
          "type": "string"
        }
      },
      "required": [
        "open"
      ],
      "additionalProperties": false
    }
  }
}